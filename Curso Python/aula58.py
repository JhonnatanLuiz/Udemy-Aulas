'''
Interpretador do Python
O interpretador do Python é um programa que executa o código Python. Ele lê o código fonte, converte-o em bytecode e o executa. O interpretador pode ser usado interativamente ou para executar scripts Python.
O interpretador do Python é escrito em C e é uma implementação da linguagem Python. Existem várias implementações do Python, incluindo CPython (a implementação padrão), Jython (Python em Java), IronPython (Python em .NET) e PyPy (uma implementação otimizada do Python).
# O interpretador do Python é uma ferramenta poderosa que permite executar código Python de forma rápida e eficiente. Ele é amplamente utilizado em desenvolvimento web, ciência de dados, aprendizado de máquina e automação de tarefas.

#python mod.py (executa o arquivo mod.py)
#python -u (unbuffered) (executa o arquivo sem buffer)
# python -m (módulo) (executa o módulo especificado)
# python -c (comando) (executa o comando especificado)
# python -i (interactive) (inicia o interpretador interativo após a execução do arquivo)
# python -V (versão) (exibe a versão do Python)
# python -h (ajuda) (exibe a ajuda do interpretador)
# python -m venv (cria um ambiente virtual)

The Zen of Python, por Tim Peters

O Zen of Python é uma coleção de princípios que influenciam o design da linguagem Python. 
Ele foi escrito por Tim Peters e é frequentemente citado como uma referência para boas práticas 
de programação em Python. O Zen of Python pode ser acessado no interpretador Python digitando "import this".

# Aqui estão os princípios do Zen of Python:
# 1. Beautiful is better than ugly.
# 2. Explicit is better than implicit.
# 3. Simple is better than complex.
# 4. Complex is better than complicated.
# 5. Flat is better than nested.
# 6. Sparse is better than dense.
# 7. Readability counts.
# 8. Special cases aren't special enough to break the rules.
# 9. Although practicality beats purity.
# 10. Errors should never pass silently.
# 11. Unless explicitly silenced.
# 12. In the face of ambiguity, refuse the temptation to guess.
# 13. There should be one-- and preferably only one --obvious way to do it.
# 14. Although that way may not be obvious at first unless you're Dutch.
# 15. Now is better than never.
# 16. Although never is often better than right now.
# 17. If the implementation is hard to explain, it's a bad idea.
# 18. If the implementation is easy to explain, it may be a good idea.
# 19. Namespaces are one honking great idea -- let's do more of those!
'''